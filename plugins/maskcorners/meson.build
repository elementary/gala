maskcorners_files = [
    'Main.vala',
    'Settings.vala'
]

shared_module('gala-maskcorners', maskcorners_files,
              dependencies: [core_dependencies, gala_dependencies],
              include_directories: libgala_dir,
              install: true,
              install_dir: plugin_dir)
