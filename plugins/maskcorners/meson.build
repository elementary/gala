maskcorners_files = files([
    'Main.vala',
    'Settings.vala'
])

shared_module(
    'gala-maskcorners',
    maskcorners_files,
    dependencies: [core_dependencies, gala_dependencies],
    install: true,
    install_dir: plugin_dir)
